"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getSdk;
exports.clearSdkForTest = clearSdkForTest;
// TODO(alex): prevent sdk sharing across invocations of the same lambda container
var sdk = null;
/** @hidden */

function getSdk() {
  if (!sdk) {
    var getAirtableInterface = require('./injected/airtable_interface').default;

    var Sdk = require('./sdk').default;

    sdk = new Sdk(getAirtableInterface());
  }

  return sdk;
}
/** @hidden */


function clearSdkForTest() {
  sdk = null;
}